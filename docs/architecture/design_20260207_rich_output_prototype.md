# 🎨 Design: リッチアウトプット機能（プロトタイプ）

## プロジェクトコンテキスト
- **技術スタック**: Next.js 16 + TypeScript + Supabase
- **フェーズ**: Phase C（プロトタイプ）
- **目的**: AIチャットから実際のPPTファイルを生成できることを検証

---

## 要件サマリー

### ユーザーの要望
> AIに依頼したら、テキストではなく実際の成果物（PPT/フォーム/プラン）が生成される

### プロトタイプの範囲
- **入力**: AI相談画面で「営業計画のPPTを作って」等の依頼
- **処理**: 固定テンプレートでPPTX生成
- **出力**: ダウンロードリンク表示

### スコープ外（Phase C）
- ❌ Difyツール統合（まずは手動トリガー）
- ❌ 動的コンテンツ生成（固定テンプレート）
- ❌ 複雑なデザイン（シンプルな2-3スライド）

---

## アーキテクチャ図

```
┌─────────────────────────────────────────┐
│  AI相談画面                              │
│  ┌─────────────────────────────────┐   │
│  │  AIとの会話                      │   │
│  │  「営業計画のPPTを作って」       │   │
│  └─────────────────────────────────┘   │
│                ↓                         │
│  ┌─────────────────────────────────┐   │
│  │  [テスト] PPT生成ボタン         │   │ ← プロトタイプ専用
│  └─────────────────────────────────┘   │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│  API Route                               │
│  /api/tools/generate-presentation        │
│  ┌─────────────────────────────────┐   │
│  │  1. pptxgenjsでPPT生成         │   │
│  │  2. Buffer生成                  │   │
│  │  3. Base64エンコード            │   │
│  │  4. JSON返却                    │   │
│  └─────────────────────────────────┘   │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│  フロントエンド                          │
│  ┌─────────────────────────────────┐   │
│  │  📊 営業計画.pptx                │   │
│  │  [ダウンロード]                  │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

## モジュール設計

### 1. API Route: `/api/tools/generate-presentation/route.ts`
**責務**: PPTファイル生成

**入力**:
```typescript
{
  title?: string,  // プレゼンタイトル（オプション）
  theme?: string   // テーマ（オプション）
}
```

**出力**:
```typescript
{
  fileName: string,    // ファイル名
  base64: string,      // Base64エンコードされたファイル
  mimeType: string     // application/vnd.openxmlformats-...
}
```

**依存**: `pptxgenjs`

---

### 2. コンポーネント: `components/consulting/TestPPTButton.tsx`
**責務**: テスト用のPPT生成ボタン

**機能**:
- ボタンクリック → API呼び出し
- ローディング表示
- 成功時: ダウンロードリンク表示
- エラー時: トースト通知

**保護レベル**: レベル3（新規作成）

---

### 3. 既存ファイル修正: `app/consulting/start/page.tsx`
**変更内容**: TestPPTButtonコンポーネントを追加（開発環境のみ表示）

**保護レベル**: レベル3

---

## 技術選定

### PPT生成ライブラリ

| ライブラリ | 選定理由 | 制約 |
|-----------|---------|------|
| **pptxgenjs** | ✅ 軽量、シンプル、実績豊富 | サーバーサイドのみ |
| officegen | 多機能だが複雑 | - |
| node-pptx | メンテナンス停止 | - |

**選定**: `pptxgenjs` v3.12.0

---

### ファイル配信方式

| 方式 | Phase C採用 | 理由 |
|-----|-----------|------|
| Base64エンコード + ダウンロード | ✅ | 実装が最も簡単 |
| Supabase Storage保存 | ❌ | Phase Aで実装 |
| 一時ファイル保存 | ❌ | Vercelでは不可 |

**選定**: Base64エンコード方式

---

## データフロー

### シーケンス図

```
User                TestPPTButton           API Route              pptxgenjs
 |                       |                      |                      |
 |-- クリック ----------->|                      |                      |
 |                       |-- POST /api/tools -->|                      |
 |                       |                      |-- new pptxgen() ---->|
 |                       |                      |<-- instance ---------|
 |                       |                      |-- addSlide() ------->|
 |                       |                      |-- addText() -------->|
 |                       |                      |-- write() ---------->|
 |                       |                      |<-- Buffer -----------|
 |                       |<-- JSON (base64) ----|                      |
 |<-- ダウンロードリンク -|                      |                      |
 |                       |                      |                      |
```

---

## セキュリティ考慮点

### 1. APIレート制限
- **実装**: なし（プロトタイプ）
- **Phase A**: IP/ユーザーごとに1分間5回まで

### 2. ファイルサイズ制限
- **制限**: 10MB以内
- **理由**: Base64レスポンスサイズ制限

### 3. 入力検証
- タイトル: 100文字以内
- テーマ: 許可リストのみ

---

## ファイル変更計画

### 新規作成

#### 1. `app/api/tools/generate-presentation/route.ts`
**目的**: PPT生成API

**内容**:
```typescript
- pptxgenjsでPPT生成
- 固定テンプレート（3スライド）
- Base64エンコードして返却
```

#### 2. `components/consulting/TestPPTButton.tsx`
**目的**: テスト用ボタン

**内容**:
```typescript
- ボタンクリック処理
- API呼び出し
- ダウンロード処理
```

---

### 変更対象

#### 3. `app/consulting/start/page.tsx`
**変更内容**: TestPPTButtonを追加

**影響**: なし（このファイルのみ）

**保護レベル**: レベル3

---

### 参照のみ
- `package.json` - pptxgenjs追加確認

---

## PPTテンプレート設計

### スライド構成

**スライド1: タイトル**
```
┌─────────────────────────────────┐
│                                 │
│      AI経営コンサルティング      │
│      営業計画プレゼンテーション  │
│                                 │
│      作成日: 2026-02-07         │
└─────────────────────────────────┘
```

**スライド2: 概要**
```
┌─────────────────────────────────┐
│  営業計画の概要                  │
│  ────────────────────────────   │
│  • 目標売上: 1億円               │
│  • 期間: 2026年度                │
│  • 重点施策: 新規顧客開拓        │
└─────────────────────────────────┘
```

**スライド3: アクションプラン**
```
┌─────────────────────────────────┐
│  主要アクション                  │
│  ────────────────────────────   │
│  1. 見込み客リスト作成           │
│  2. 月次訪問計画策定             │
│  3. 提案資料の標準化             │
└─────────────────────────────────┘
```

---

## エラーハンドリング

### エラーケース

| エラー | 原因 | 対処 |
|-------|------|------|
| PPT生成失敗 | pptxgenjsエラー | 500エラー、トースト通知 |
| タイムアウト | 生成に30秒以上 | タイムアウトエラー |
| メモリ不足 | ファイルサイズ超過 | 400エラー |

---

## テスト計画

### 手動テスト

1. **正常系**
   - ボタンクリック → PPT生成 → ダウンロード成功
   - PowerPointでファイルを開いて確認

2. **エラー系**
   - 連続クリック（多重実行防止）
   - ネットワークエラー時の挙動

---

## 成功基準

### Phase C（プロトタイプ）の完了条件

- ✅ ボタンクリックでPPTファイルがダウンロードされる
- ✅ PPTファイルがPowerPointで正常に開ける
- ✅ 3スライド（タイトル、概要、アクションプラン）が含まれる
- ✅ エラー時に適切なメッセージが表示される

---

## Phase A への移行条件

Phase Cが成功したら、次の機能を追加：

1. **Difyツール統合**
   - AIが自動でツール呼び出し

2. **動的コンテンツ生成**
   - ユーザーの依頼内容を反映

3. **Supabase Storage保存**
   - ファイル永続化、履歴管理

4. **複数フォーマット対応**
   - PDF、Word等

---

## リスク管理

### 技術的リスク

| リスク | 影響 | 対策 |
|-------|------|------|
| pptxgenjsの日本語対応 | 文字化け | フォント埋め込み検証 |
| Vercelのメモリ制限 | 生成失敗 | ファイルサイズ制限 |
| Base64サイズ制限 | レスポンス失敗 | 10MB制限 |

---

## 実装後のドキュメント化

実装完了後、以下を記録：

1. 実際の生成時間（平均）
2. ファイルサイズ（平均）
3. 発生したエラーと解決方法
4. ユーザーフィードバック

---

**作成日**: 2026-02-07
**Phase**: Design
**次フェーズ**: Plan（実装計画作成）
